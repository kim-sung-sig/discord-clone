# T03 Invite Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add invite create/delete/preview/accept backend behavior and a Nuxt invite modal.

**Architecture:** Backend adds a focused invite module and boot adapter that reuses guild membership and authorization services. Frontend adds static invite UI state to preserve testable UX before API wiring.

**Tech Stack:** Java 21, Spring Boot MVC, Gradle, JUnit/MockMvc, Nuxt 3, Pinia, Vitest, Playwright.

---

## Task 1: Backend Invite Lifecycle

**Files:**
- Create: `backend/modules/invite/**`
- Modify: `settings.gradle.kts`
- Modify: `backend/boot/build.gradle.kts`
- Create: `backend/boot/src/main/java/com/example/discord/invite/InviteController.java`
- Create: `backend/boot/src/main/java/com/example/discord/invite/InviteConfiguration.java`
- Create: `backend/boot/src/test/java/com/example/discord/invite/InviteControllerTest.java`
- Modify: `backend/boot/src/test/java/com/example/discord/architecture/ArchitectureTest.java`

- [ ] **Step 1: Write failing service and MockMvc tests**

Cover expired reject, max-use race safety, accept idempotency, delete reuse rejection, unauthorized create rejection.

- [ ] **Step 2: Verify RED**

Run: `.\gradlew.bat :backend:modules:invite:test :backend:boot:test --tests com.example.discord.invite.InviteControllerTest`

- [ ] **Step 3: Implement minimal invite module and REST adapter**

Use synchronized in-memory service; accept adds member and role grants through `InMemoryGuildService`.

- [ ] **Step 4: Verify GREEN**

Run the same targeted backend tests.

## Task 2: Frontend Invite Modal

**Files:**
- Modify: `apps/web/stores/shell.ts`
- Create: `apps/web/components/invite/InviteModal.vue`
- Modify: `apps/web/pages/app.vue`
- Modify: `apps/web/assets/css/main.css`
- Modify: `apps/web/tests/components/app-shell.test.ts`
- Modify: `apps/web/tests/e2e/app-shell.spec.ts`

- [ ] **Step 1: Write failing component/e2e assertions**

Assert invite modal preview, expiry/max-use metadata, role grant skeleton, and accept CTA.

- [ ] **Step 2: Verify RED**

Run: `npm run test -w apps/web -- --run tests/components/app-shell.test.ts`

- [ ] **Step 3: Implement minimal modal and seed state**

Mount modal inside workspace.

- [ ] **Step 4: Verify GREEN**

Run component and e2e tests.

## Task 3: QA And PDCA Closure

**Files:**
- Create: `docs/03-analysis/T03-invite.analysis.md`
- Create: `docs/04-report/T03-invite.report.md`
- Create: `docs/05-feedback/T03-invite.feedback.md`

- [ ] **Step 1: Run full gates**

Run backend full tests, frontend component tests, e2e, build, and compose config.

- [ ] **Step 2: Review and fix**

Run backend/frontend reviews and close findings.

- [ ] **Step 3: Commit**

Commit with message `feat: add invite lifecycle slice`.

