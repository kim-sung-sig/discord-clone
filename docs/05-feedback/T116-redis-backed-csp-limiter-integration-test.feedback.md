# T116 Redis-backed CSP Limiter Integration Test Feedback

## Improvement Tasks Captured

### T123 CI Docker Test Toggle

Add CI documentation or workflow configuration that runs Docker-backed web integration tests with `NUXT_RUN_DOCKER_TESTS=true` when Docker service is available.

