spring:
  autoconfigure:
    exclude: []
  datasource:
    url: ${POSTGRES_JDBC_URL:jdbc:postgresql://127.0.0.1:15432/discord_websocket}
    username: ${POSTGRES_USER:dev_user}
  password: ${POSTGRES_PASSWORD}
  flyway:
    locations: classpath:db/migration
    baseline-on-migrate: true
