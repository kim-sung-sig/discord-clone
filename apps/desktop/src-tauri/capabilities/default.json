{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop shell boundary. No filesystem, shell, or HTTP permissions are enabled.",
  "windows": ["main"],
  "permissions": []
}
